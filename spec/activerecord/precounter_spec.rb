require "spec_helper"

RSpec.describe Activerecord::Precounter do
  it "has a version number" do
    expect(Activerecord::Precounter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
